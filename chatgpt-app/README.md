# Workflow Wizard MCP

Kør den lokale MCP-server:

```bash
cd chatgpt-app
npm install
npm start
```

Serveren eksponerer MCP-endpointet på `http://localhost:8787/mcp` (GET `/` svarer med en simpel status).
